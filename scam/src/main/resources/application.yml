server:
  port: 8081
spring:
  application:
    name: scam
  datasource:
    password: '1234'
    url: jdbc:postgresql://localhost:5432/scam
    username: 'postgres'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true